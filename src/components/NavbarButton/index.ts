import { styled as MUIstyled } from "@mui/material/styles";
import Button from "@mui/material/Button";

export const NavbarButton = MUIstyled(Button)({
    color:'white',
    textTransform : 'none'
});
